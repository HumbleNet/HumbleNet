namespace TestClient;

table HelloPeer {
	peers: [uint32];
	is_response: bool;
}

table Chat {
	message: string;
}

union MessageSwitch {
	HelloPeer,
	Chat
}

table Message {
	message: MessageSwitch (required);
}

root_type Message;